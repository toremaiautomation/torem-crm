# Copy to .env.local and fill in. Never commit .env.local.
VITE_SUPABASE_URL=https://qcqkkpivbqxoshfsgnah.supabase.co
# Publishable/anon key from Supabase → Settings → API. NOT the service_role/secret key.
VITE_SUPABASE_ANON_KEY=
# true = seeded demo data + fake login (no Supabase needed). false = real Supabase.
VITE_USE_MOCK=true
